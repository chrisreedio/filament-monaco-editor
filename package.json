{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node bin/build.js --dev",
        "build": "node bin/build.js"
    },
    "dependencies": {
        "monaco-editor": "^0.55.0"
    },
    "devDependencies": {
        "esbuild": "^0.25.5",
        "prettier": "^3.5.3"
    }
}
